name: Deploy to Amazon EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu  # Standard for AWS Ubuntu instances
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Navigate to home directory
            cd /home/ubuntu
            
            # Clone if doesn't exist, otherwise pull
            if [ ! -d "core-project" ]; then
              git clone git@github.com:Dhruvjalan/Core_App_Project.git core-project
            fi
            
            cd core-project
            git pull origin main
            npm install --production
            
            # Restart the app with PM2
            pm2 delete core-app || true
            pm2 start src/app.js --name "core-app"
            pm2 save